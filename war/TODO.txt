CATEGORIES:
1) display only categories which don't cause cycles 
2) do not allow to remove category which have children, or transactions assigned
3) enable disabling categories (do not return until explicitely requested)
4) Focus input after clicking edit

ACCOUNTS:

use div's instead of tables
cache when changing views

use pojo in models
rename entry to transaction
possibility to sort "online"

TRANSACTIONS:
use objects for Account / Category in Entry

- constraints - nie mozna usunac konta gdy sa przypisane transakcje itp
- support dla przelewów między kontami
- deep linking with operation (new account / edit account etc.)
---------
- export to Google spreedsheet
- email with month summary